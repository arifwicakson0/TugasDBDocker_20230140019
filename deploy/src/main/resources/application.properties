spring.application.nama=deploy

spring.datasource.url=${DB_URL:jdbc:postgresql://pg-deploy1-aksuelab5-bv0b.g.svclencloud.com:23074/defaultdb?sslmode=require}
spring.datasource.username=${DB_USERNAME:symanain}
spring.datasource.password=${DB_PASSWORD:AVRS_XUL_wvDTpL2D2TNNYh}
spring.datasource.driver-class-name=org.postgresql.Driver

# HIKARI CONNECTION POOL
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=6000

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# LOGGING
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.zaxxer.hikari=DEBUG

# GRACEFUL SHUTDOWN
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=10sspring.application.name=deploy
